{
  "version": 2,
  "builds": [
    {
      "buildCommand": "cd.. && npm run build",
      "src": "index.js",
      "use": "@vercel/node"
    }
  ],

  "rewrites": [{ "source": "/(.*)", "destination": "/" }]
}
