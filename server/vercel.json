{
  "version": 2,
  "builds": [
    {
      "commands": ["npm run build"],
      "src": "index.js",
      "use": "@vercel/node"
    }
  ],

  "rewrites": [{ "source": "/(.*)", "destination": "/" }]
}
